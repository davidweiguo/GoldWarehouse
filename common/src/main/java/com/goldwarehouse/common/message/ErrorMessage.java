package com.goldwarehouse.common.message;

public enum ErrorMessage {
    // exception start with 0
    EXCEPTION_MESSAGE,
    NONE_MATCHED_MESSAGE,
    EMPTY_MESSAGE,
    // system errors start with 100
}
